---
description: Token随手记 uni-app 跨端编码规范与自动检查
globs: "**/*.{vue,js,scss,css}"
alwaysApply: true
---

# Token随手记 — Agent 编码守则

## 平台目标
Android / iOS / 微信小程序 / H5(Web) — 详见 `docs/代码规范.md`

## 必守规则
1. 使用 `uni.*` API，禁止 `document`/`window`/`localStorage`（H5 专用须 `#ifdef H5`）
2. 布局尺寸用 `rpx`，颜色/圆角用 `@/styles/variables.scss` 变量
3. 平台差异用条件编译 `#ifdef`，禁止运行时平台分支调用 DOM
4. 组件顺序：template → script → style；组件名 PascalCase
5. 网络请求走 `@/utils/request.js`，禁止 fetch/axios

## 每次改码后必须执行
```bash
npm run check
```
有 error 则修复后重跑，直至通过。不可跳过。

## 依赖参考
- UI: uView UI 2 (`uview-ui`)
- 图表: qiun-data-charts (`uni_modules`)
- 状态: Vuex 3 | 请求: luch-request | 日期: dayjs

完整说明见 `docs/依赖选型.md` 与 `docs/代码规范.md`。
